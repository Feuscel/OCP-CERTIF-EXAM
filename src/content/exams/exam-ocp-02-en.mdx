---
id: "exam-ocp-02"
title: "Java OCP Exam 02"
lang: "en"
duration_minutes: 60
shuffle_questions: true
shuffle_options: true
difficulty: "medium"
tags: ["generics", "collections"]
questions:
  - id: "ocp-02-001"
    title_fr: "Génériques et effacement de type\n```java\nList<String> a = new ArrayList<>();\nList<Object> b = a;\n```\nQue se passe-t-il ?"
    title_en: "Generics and Type Erasure\n```java\nList<String> a = new ArrayList<>();\nList<Object> b = a;\n```\nWhat happens?"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "Le code compile et s'exécute sans erreur."
        text_en: "The code compiles and runs without error."
      - label: "B"
        text_fr: "Erreur de compilation : types incompatibles. Utiliser `List<String>` et non `List<Object>`."
        text_en: "Compile error: incompatible types. Use `List<String>` not `List<Object>`."
      - label: "C"
        text_fr: "ClassCastException à l'exécution."
        text_en: "ClassCastException at runtime."
      - label: "D"
        text_fr: "La liste accepte tout type."
        text_en: "The list accepts any type."
    correct_answers: ["B"]
    explanation_fr: "L'effacement de type rend les génériques invariants à la compilation : `List<String>` ne peut pas recevoir `List<Object>`."
    explanation_en: "Type erasure makes generics invariant at compile time: `List<String>` cannot accept `List<Object>`."
  - id: "ocp-02-002"
    title_fr: "Collections et equals/hashCode"
    title_en: "Collections and equals/hashCode"
    type: "multiple-choice"
    options:
      - label: "A"
        text_fr: "HashSet utilise equals() pour vérifier les doublons."
        text_en: "HashSet uses equals() to check duplicates."
      - label: "B"
        text_fr: "HashMap ignore entièrement hashCode."
        text_en: "HashMap ignores hashCode entirely."
      - label: "C"
        text_fr: "Surcharger equals sans hashCode casse les recherches dans HashMap."
        text_en: "Overriding equals without hashCode breaks HashMap lookups."
      - label: "D"
        text_fr: "Deux objets égaux doivent avoir le même hashCode."
        text_en: "Two equal objects must have the same hashCode."
    correct_answers: ["A", "C", "D"]
    explanation_fr: "Les collections basées sur le hachage s'appuient sur hashCode() puis equals(). Violater le contrat equals/hashCode casse les recherches."
    explanation_en: "Hash-based collections rely on hashCode() then equals(). Violating the equals/hashCode contract breaks lookups."
---

Java OCP Exam 02 (EN) content.