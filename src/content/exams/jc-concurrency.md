---
id: "jc-concurrency"
title: "Java21DocCards - Concurrency"
lang: "en"
duration_minutes: 15
shuffle_questions: true
shuffle_options: true
difficulty: "medium"
tags: ["concurrency", "threads", "executors", "locks"]
questions:
  - id: "jc-concurrency-001"
    title_fr: "What is the output of this ExecutorService code?\n```java\nimport java.util.concurrent.*;\npublic class Test {\n    public static void main(String[] args) throws Exception {\n        ExecutorService service = Executors.newFixedThreadPool(1);\n        Runnable r = () -> System.out.print(\"Runnable executed\");\n        Callable<String> c = () -> \"Callable executed\";\n\n        Future<?> f1 = service.submit(r);\n        Future<String> f2 = service.submit(c);\n\n        System.out.print(f1.get());\n        System.out.print(f2.get());\n        service.shutdown();\n    }\n}\n```"
    title_en: "What is the output of this ExecutorService code?\n```java\nimport java.util.concurrent.*;\npublic class Test {\n    public static void main(String[] args) throws Exception {\n        ExecutorService service = Executors.newFixedThreadPool(1);\n        Runnable r = () -> System.out.print(\"Runnable executed\");\n        Callable<String> c = () -> \"Callable executed\";\n\n        Future<?> f1 = service.submit(r);\n        Future<String> f2 = service.submit(c);\n\n        System.out.print(f1.get());\n        System.out.print(f2.get());\n        service.shutdown();\n    }\n}\n```"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "Runnable executedCallable executed"
        text_en: "Runnable executedCallable executed"
      - label: "B"
        text_fr: "Runnable executednullCallable executed"
        text_en: "Runnable executednullCallable executed"
      - label: "C"
        text_fr: "nullCallable executed"
        text_en: "nullCallable executed"
      - label: "D"
        text_fr: "Compilation error"
        text_en: "Compilation error"
      - label: "E"
        text_fr: "Runtime exception"
        text_en: "Runtime exception"
    correct_answers: ["B"]
    explanation_fr: "submit(Runnable) returns a Future<?> whose get() returns null upon successful completion. submit(Callable<T>) returns a Future<T> whose get() returns the callable result. The output is: 'Runnable executed' (from the Runnable), then 'null' (from f1.get()), then 'Callable executed' (from f2.get()) - all printed consecutively without newlines since print() is used."
    explanation_en: "submit(Runnable) returns a Future<?> whose get() returns null upon successful completion. submit(Callable<T>) returns a Future<T> whose get() returns the callable result. The output is: 'Runnable executed' (from the Runnable), then 'null' (from f1.get()), then 'Callable executed' (from f2.get()) - all printed consecutively without newlines since print() is used."
  - id: "jc-concurrency-002"
    title_fr: "What is the output of this ReentrantLock code?\n```java\nimport java.util.concurrent.*;\nimport java.util.concurrent.locks.*;\n\npublic class FamilyGathering {\n    private Lock dinnerLock = new ReentrantLock();\n    \n    public void joinDinner() {\n        dinnerLock.lock();           // f1\n        if (dinnerLock.tryLock()) {  // f2\n            System.out.print(\"Eating dinner!\");\n        }\n        dinnerLock.unlock();\n    }\n    \n    public static void main(String[] args) {\n        try (var service = Executors.newFixedThreadPool(2)) {\n            FamilyGathering family = new FamilyGathering();\n            for (int i = 0; i < 2; i++)\n                service.submit(() -> family.joinDinner());\n        }\n        System.out.print(\"Family dinner complete\");\n    }\n}\n```"
    title_en: "What is the output of this ReentrantLock code?\n```java\nimport java.util.concurrent.*;\nimport java.util.concurrent.locks.*;\n\npublic class FamilyGathering {\n    private Lock dinnerLock = new ReentrantLock();\n    \n    public void joinDinner() {\n        dinnerLock.lock();           // f1\n        if (dinnerLock.tryLock()) {  // f2\n            System.out.print(\"Eating dinner!\");\n        }\n        dinnerLock.unlock();\n    }\n    \n    public static void main(String[] args) {\n        try (var service = Executors.newFixedThreadPool(2)) {\n            FamilyGathering family = new FamilyGathering();\n            for (int i = 0; i < 2; i++)\n                service.submit(() -> family.joinDinner());\n        }\n        System.out.print(\"Family dinner complete\");\n    }\n}\n```"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "Eating dinner!Family dinner complete"
        text_en: "Eating dinner!Family dinner complete"
      - label: "B"
        text_fr: "Eating dinner!Eating dinner!Family dinner complete"
        text_en: "Eating dinner!Eating dinner!Family dinner complete"
      - label: "C"
        text_fr: "Family dinner complete"
        text_en: "Family dinner complete"
      - label: "D"
        text_fr: "Program hangs and prints only: Eating dinner!"
        text_en: "Program hangs and prints only: Eating dinner!"
      - label: "E"
        text_fr: "Compilation error"
        text_en: "Compilation error"
      - label: "F"
        text_fr: "Runtime exception"
        text_en: "Runtime exception"
    correct_answers: ["D"]
    explanation_fr: "This program demonstrates a ReentrantLock bug that causes a deadlock. When the first thread calls dinnerLock.lock() at f1, it acquires the lock. Then at f2, dinnerLock.tryLock() succeeds because ReentrantLock allows the same thread to acquire the lock multiple times (reentrant behavior). So 'Eating dinner!' prints once. However, the thread acquired the lock twice (once at f1, once at f2) but only calls unlock() once. This means the lock is never fully released - it still has a hold count of 1. When the first thread exits, the lock remains held, causing the second thread to wait indefinitely at its dinnerLock.lock() call. The program hangs and never prints 'Family dinner complete' because the ExecutorService cannot shut down with a blocked thread."
    explanation_en: "This program demonstrates a ReentrantLock bug that causes a deadlock. When the first thread calls dinnerLock.lock() at f1, it acquires the lock. Then at f2, dinnerLock.tryLock() succeeds because ReentrantLock allows the same thread to acquire the lock multiple times (reentrant behavior). So 'Eating dinner!' prints once. However, the thread acquired the lock twice (once at f1, once at f2) but only calls unlock() once. This means the lock is never fully released - it still has a hold count of 1. When the first thread exits, the lock remains held, causing the second thread to wait indefinitely at its dinnerLock.lock() call. The program hangs and never prints 'Family dinner complete' because the ExecutorService cannot shut down with a blocked thread."
  - id: "jc-concurrency-003"
    title_fr: "What are the states of the threads after the main thread calls dad.interrupt()?\n```java\nclass FamilyMember extends Thread {\n    private String name;\n    private static final Object kitchen = new Object();\n    \n    FamilyMember(String name) { this.name = name; }\n    \n    public void run() {\n        System.out.println(name + \" wants to cook\");\n        synchronized(kitchen) {\n            System.out.println(name + \" is cooking\");\n            try {\n                Thread.sleep(5000); // cook for 5 seconds\n            } catch(InterruptedException e) {\n                System.out.println(name + \" interrupted while cooking\");\n            }\n            System.out.println(name + \" finished cooking\");\n        }\n    }\n}\n\npublic class FamilyDinner {\n    public static void main(String[] args) throws Exception {\n        FamilyMember mom = new FamilyMember(\"Mom\");\n        FamilyMember dad = new FamilyMember(\"Dad\");\n        \n        mom.start();\n        Thread.sleep(100); // ensure mom gets kitchen first\n        dad.start();\n        Thread.sleep(100); // ensure dad is blocked\n        dad.interrupt();\n        \n        // What are the thread states here?\n    }\n}\n```"
    title_en: "What are the states of the threads after the main thread calls dad.interrupt()?\n```java\nclass FamilyMember extends Thread {\n    private String name;\n    private static final Object kitchen = new Object();\n    \n    FamilyMember(String name) { this.name = name; }\n    \n    public void run() {\n        System.out.println(name + \" wants to cook\");\n        synchronized(kitchen) {\n            System.out.println(name + \" is cooking\");\n            try {\n                Thread.sleep(5000); // cook for 5 seconds\n            } catch(InterruptedException e) {\n                System.out.println(name + \" interrupted while cooking\");\n            }\n            System.out.println(name + \" finished cooking\");\n        }\n    }\n}\n\npublic class FamilyDinner {\n    public static void main(String[] args) throws Exception {\n        FamilyMember mom = new FamilyMember(\"Mom\");\n        FamilyMember dad = new FamilyMember(\"Dad\");\n        \n        mom.start();\n        Thread.sleep(100); // ensure mom gets kitchen first\n        dad.start();\n        Thread.sleep(100); // ensure dad is blocked\n        dad.interrupt();\n        \n        // What are the thread states here?\n    }\n}\n```"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "Mom: TIMED_WAITING, Dad: RUNNABLE, Main: RUNNABLE"
        text_en: "Mom: TIMED_WAITING, Dad: RUNNABLE, Main: RUNNABLE"
      - label: "B"
        text_fr: "Mom: TIMED_WAITING, Dad: BLOCKED, Main: RUNNABLE"
        text_en: "Mom: TIMED_WAITING, Dad: BLOCKED, Main: RUNNABLE"
      - label: "C"
        text_fr: "Mom: RUNNABLE, Dad: TERMINATED, Main: RUNNABLE"
        text_en: "Mom: RUNNABLE, Dad: TERMINATED, Main: RUNNABLE"
      - label: "D"
        text_fr: "Mom: BLOCKED, Dad: RUNNABLE, Main: TIMED_WAITING"
        text_en: "Mom: BLOCKED, Dad: RUNNABLE, Main: TIMED_WAITING"
      - label: "E"
        text_fr: "Mom: TIMED_WAITING, Dad: WAITING, Main: RUNNABLE"
        text_en: "Mom: TIMED_WAITING, Dad: WAITING, Main: RUNNABLE"
    correct_answers: ["B"]
    explanation_fr: "Mom acquired the kitchen lock and is sleeping (TIMED_WAITING). Dad is blocked waiting for the kitchen lock (BLOCKED). Interrupting a BLOCKED thread does NOT throw InterruptedException - Dad remains BLOCKED until he can acquire the lock, then he can check interrupt status. Main thread continues executing (RUNNABLE). Key point: interrupt() only throws InterruptedException for threads in WAITING or TIMED_WAITING states, not BLOCKED."
    explanation_en: "Mom acquired the kitchen lock and is sleeping (TIMED_WAITING). Dad is blocked waiting for the kitchen lock (BLOCKED). Interrupting a BLOCKED thread does NOT throw InterruptedException - Dad remains BLOCKED until he can acquire the lock, then he can check interrupt status. Main thread continues executing (RUNNABLE). Key point: interrupt() only throws InterruptedException for threads in WAITING or TIMED_WAITING states, not BLOCKED."
---

Java21DocCards - Concurrency questions.