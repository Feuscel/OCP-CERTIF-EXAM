---
id: "ch04"
title: "Chapter 4 - String Processing"
lang: "en"
duration_minutes: 15
shuffle_questions: true
shuffle_options: true
difficulty: "medium"
tags: ["string", "stringbuilder", "regex"]
questions:
  - id: "ch04-001"
    title_fr: "Which of the following statements about Java primitive and reference data types is true?"
    title_en: "Which of the following statements about Java primitive and reference data types is true?"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "A `double` can be directly assigned to a `float` without casting."
        text_en: "A `double` can be directly assigned to a `float` without casting."
      - label: "B"
        text_fr: "A `boolean` can be cast to an `int`."
        text_en: "A `boolean` can be cast to an `int`."
      - label: "C"
        text_fr: "A `String` can be assigned to an `Object` reference variable."
        text_en: "A `String` can be assigned to an `Object` reference variable."
      - label: "D"
        text_fr: "A `char` is a reference data type."
        text_en: "A `char` is a reference data type."
      - label: "E"
        text_fr: "An `int` can store a `long` value without any explicit casting."
        text_en: "An `int` can store a `long` value without any explicit casting."
    correct_answers: ["C"]
    explanation_fr: "A) Incorrect: A `double` cannot be directly assigned to a `float` without casting because `double` has a larger range and precision than a `float`. B) Incorrect: `boolean` values cannot be cast to `int` in Java. They are not compatible types. C) Correct: A `String` is an instance of the `Object` class, and hence it can be assigned to an `Object` reference variable. D) Incorrect: `char` is a primitive data type, not a reference data type. E) Incorrect: an `int` cannot store a `long` value without explicit casting because `long` has a larger range than `int`."
    explanation_en: "A) Incorrect: A `double` cannot be directly assigned to a `float` without casting because `double` has a larger range and precision than a `float`. B) Incorrect: `boolean` values cannot be cast to `int` in Java. They are not compatible types. C) Correct: A `String` is an instance of the `Object` class, and hence it can be assigned to an `Object` reference variable. D) Incorrect: `char` is a primitive data type, not a reference data type. E) Incorrect: an `int` cannot store a `long` value without explicit casting because `long` has a larger range than `int`."
  - id: "ch04-002"
    title_fr: "What is the output of the following code snippet?"
    title_en: "What is the output of the following code snippet?\n```java\npublic class OperatorTest {\n    public static void main(String[] args) {\n        int a = 5;\n        int b = 10;\n        int c = 15;\n        int result = a + b * c / a - b;\n        System.out.println(result);\n    }\n}\n```"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "`25`"
        text_en: "`25`"
      - label: "B"
        text_fr: "`35`"
        text_en: "`35`"
      - label: "C"
        text_fr: "`20`"
        text_en: "`20`"
      - label: "D"
        text_fr: "`15`"
        text_en: "`15`"
    correct_answers: ["A"]
    explanation_fr: ''
    explanation_en: ''
  - id: "ch04-003"
    title_fr: "Which of the following statements about `String` and `StringBuilder` is true?"
    title_en: "Which of the following statements about `String` and `StringBuilder` is true?"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "`StringBuilder` objects are immutable."
        text_en: "`StringBuilder` objects are immutable."
      - label: "B"
        text_fr: "`String` objects can be modified after they are created."
        text_en: "`String` objects can be modified after they are created."
      - label: "C"
        text_fr: "`StringBuilder` is synchronized and thread-safe."
        text_en: "`StringBuilder` is synchronized and thread-safe."
      - label: "D"
        text_fr: "`StringBuilder` provides methods for mutable sequence of characters."
        text_en: "`StringBuilder` provides methods for mutable sequence of characters."
      - label: "E"
        text_fr: "`String` and `StringBuilder` have the same performance characteristics for string manipulation."
        text_en: "`String` and `StringBuilder` have the same performance characteristics for string manipulation."
    correct_answers: ["D"]
    explanation_fr: "A) Incorrect: `StringBuilder` objects are mutable, meaning they can be changed after they are created. B) Incorrect: `String` objects are immutable, meaning once a `String` object is created, it cannot be modified. Any modification results in a new `String` object. C) Incorrect: `StringBuilder` is not synchronized and is not thread-safe. If synchronization is required, `StringBuffer` should be used instead. D) Correct: `StringBuilder` provides methods for a mutable sequence of characters, allowing for modification of the object without creating new instances. E) Incorrect: `String` and `StringBuilder` do not have the same performance characteristics for string manipulation. `StringBuilder` is generally more efficient for such operations because it is mutable and does not create new instances with each modification."
    explanation_en: "A) Incorrect: `StringBuilder` objects are mutable, meaning they can be changed after they are created. B) Incorrect: `String` objects are immutable, meaning once a `String` object is created, it cannot be modified. Any modification results in a new `String` object. C) Incorrect: `StringBuilder` is not synchronized and is not thread-safe. If synchronization is required, `StringBuffer` should be used instead. D) Correct: `StringBuilder` provides methods for a mutable sequence of characters, allowing for modification of the object without creating new instances. E) Incorrect: `String` and `StringBuilder` do not have the same performance characteristics for string manipulation. `StringBuilder` is generally more efficient for such operations because it is mutable and does not create new instances with each modification."
  - id: "ch04-004"
    title_fr: "Which of the following statements about text blocks are true? (Choose all that apply.)"
    title_en: "Which of the following statements about text blocks are true? (Choose all that apply.)"
    type: "multiple-choice"
    options:
      - label: "A"
        text_fr: "Text blocks can span multiple lines without needing escape sequences for new lines."
        text_en: "Text blocks can span multiple lines without needing escape sequences for new lines."
      - label: "B"
        text_fr: "Text blocks preserve the exact format, including whitespace, of the code as written."
        text_en: "Text blocks preserve the exact format, including whitespace, of the code as written."
      - label: "C"
        text_fr: "Text blocks can only be used within methods."
        text_en: "Text blocks can only be used within methods."
      - label: "D"
        text_fr: "Text blocks automatically trim leading and trailing whitespace from each line."
        text_en: "Text blocks automatically trim leading and trailing whitespace from each line."
      - label: "E"
        text_fr: "Text blocks require a minimum indentation level of one space."
        text_en: "Text blocks require a minimum indentation level of one space."
    correct_answers: ["A", "B"]
    explanation_fr: "A) Correct: Text blocks can indeed span multiple lines without needing escape sequences for new lines, making it easier to work with multi-line strings. B) Correct: Text blocks preserve the exact format, including whitespace, of the code as written. This is useful for maintaining the original layout of the text. C) Incorrect: Text blocks can be used anywhere a regular `String` can be used, not just within methods. They can be part of class fields, method parameters, etc. D) Incorrect: Text blocks do not automatically trim leading and trailing whitespace from each line. They preserve the exact whitespace as written in the code. E) Incorrect: Text blocks do not require a minimum indentation level of one space. The leading indentation common to all lines is removed automatically, but lines within the text block can have zero or more leading spaces."
    explanation_en: "A) Correct: Text blocks can indeed span multiple lines without needing escape sequences for new lines, making it easier to work with multi-line strings. B) Correct: Text blocks preserve the exact format, including whitespace, of the code as written. This is useful for maintaining the original layout of the text. C) Incorrect: Text blocks can be used anywhere a regular `String` can be used, not just within methods. They can be part of class fields, method parameters, etc. D) Incorrect: Text blocks do not automatically trim leading and trailing whitespace from each line. They preserve the exact whitespace as written in the code. E) Incorrect: Text blocks do not require a minimum indentation level of one space. The leading indentation common to all lines is removed automatically, but lines within the text block can have zero or more leading spaces."
  - id: "ch04-005"
    title_fr: "Which of the following statements about the `Math` class is true?"
    title_en: "Which of the following statements about the `Math` class is true?"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "The `Math.round()` method returns a `double`."
        text_en: "The `Math.round()` method returns a `double`."
      - label: "B"
        text_fr: "The `Math.random()` method returns a random integer."
        text_en: "The `Math.random()` method returns a random integer."
      - label: "C"
        text_fr: "The `Math.max()` method can only be used with integers."
        text_en: "The `Math.max()` method can only be used with integers."
      - label: "D"
        text_fr: "The `Math.pow()` method returns the result of raising the first argument to the power of the second argument."
        text_en: "The `Math.pow()` method returns the result of raising the first argument to the power of the second argument."
      - label: "E"
        text_fr: "The `Math.abs()` method can only be used with positive numbers."
        text_en: "The `Math.abs()` method can only be used with positive numbers."
    correct_answers: ["D"]
    explanation_fr: "A) Incorrect: The `Math.round()` method returns a `long` when given a `double` argument and an `int` when given a `float` argument. B) Incorrect: The `Math.random()` method returns a `double` value between 0.0 (inclusive) and 1.0 (exclusive). C) Incorrect: The `Math.max()` method can be used with various numeric types, including `int`, `long`, `float`, and `double`. D) Correct: The `Math.pow()` method returns the result of raising the first argument to the power of the second argument. Both arguments are of type `double`. E) Incorrect: The `Math.abs()` method can be used with negative numbers to return their absolute value, and it works with various numeric types including `int`, `long`, `float`, and `double`."
    explanation_en: "A) Incorrect: The `Math.round()` method returns a `long` when given a `double` argument and an `int` when given a `float` argument. B) Incorrect: The `Math.random()` method returns a `double` value between 0.0 (inclusive) and 1.0 (exclusive). C) Incorrect: The `Math.max()` method can be used with various numeric types, including `int`, `long`, `float`, and `double`. D) Correct: The `Math.pow()` method returns the result of raising the first argument to the power of the second argument. Both arguments are of type `double`. E) Incorrect: The `Math.abs()` method can be used with negative numbers to return their absolute value, and it works with various numeric types including `int`, `long`, `float`, and `double`."
---

Chapter 4 - String Processing exam.