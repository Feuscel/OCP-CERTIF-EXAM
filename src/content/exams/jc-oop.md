---
id: "jc-oop"
title: "Java21DocCards - OOP"
lang: "en"
duration_minutes: 15
shuffle_questions: true
shuffle_options: true
difficulty: "medium"
tags: ["oop", "inheritance", "polymorphism"]
questions:
  - id: "jc-oop-001"
    title_fr: "Which method calls will execute in this inheritance scenario?\n```java\nclass Parent {\n    public void show() { System.out.print(\"Parent \"); }\n}\nclass Child extends Parent {\n    public void show() { System.out.print(\"Child \"); }\n    public static void main(String[] args) {\n        Parent dad = new Child();\n        dad.show();\n        new Child().show();\n        new Parent().show();\n    }\n}\n```"
    title_en: "Which method calls will execute in this inheritance scenario?\n```java\nclass Parent {\n    public void show() { System.out.print(\"Parent \"); }\n}\nclass Child extends Parent {\n    public void show() { System.out.print(\"Child \"); }\n    public static void main(String[] args) {\n        Parent dad = new Child();\n        dad.show();\n        new Child().show();\n        new Parent().show();\n    }\n}\n```"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "Parent Parent Parent"
        text_en: "Parent Parent Parent"
      - label: "B"
        text_fr: "Child Child Parent"
        text_en: "Child Child Parent"
      - label: "C"
        text_fr: "Child Parent Child"
        text_en: "Child Parent Child"
      - label: "D"
        text_fr: "Parent Child Parent"
        text_en: "Parent Child Parent"
      - label: "E"
        text_fr: "None of the above"
        text_en: "None of the above"
    correct_answers: ["B"]
    explanation_fr: "Overridden methods in subclasses are executed according to the runtime type of the object."
    explanation_en: "Overridden methods in subclasses are executed according to the runtime type of the object."
  - id: "jc-oop-002"
    title_fr: "How are static and instance initialization blocks executed in this inheritance scenario?\n```java\nclass Parent {\n    static { System.out.print(\"Parent static \"); }\n    { System.out.print(\"Parent instance \"); }\n}\nclass Child extends Parent {\n    static { System.out.print(\"Child static \"); }\n    { System.out.print(\"Child instance \"); }\n    public static void main(String[] args) { new Child(); }\n}\n```"
    title_en: "How are static and instance initialization blocks executed in this inheritance scenario?\n```java\nclass Parent {\n    static { System.out.print(\"Parent static \"); }\n    { System.out.print(\"Parent instance \"); }\n}\nclass Child extends Parent {\n    static { System.out.print(\"Child static \"); }\n    { System.out.print(\"Child instance \"); }\n    public static void main(String[] args) { new Child(); }\n}\n```"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "Parent static Child static Child instance Parent instance"
        text_en: "Parent static Child static Child instance Parent instance"
      - label: "B"
        text_fr: "Parent static Child static Parent instance Child instance"
        text_en: "Parent static Child static Parent instance Child instance"
      - label: "C"
        text_fr: "Child static Parent static Child instance Parent instance"
        text_en: "Child static Parent static Child instance Parent instance"
      - label: "D"
        text_fr: "Parent instance Child instance Parent static Child static"
        text_en: "Parent instance Child instance Parent static Child static"
    correct_answers: ["B"]
    explanation_fr: "Static blocks execute first (parent then child), followed by instance blocks (parent then child)."
    explanation_en: "Static blocks execute first (parent then child), followed by instance blocks (parent then child)."
  - id: "jc-oop-003"
    title_fr: "Which private method is invoked in this inheritance scenario?\n```java\nclass Parent {\n    private void secret() { System.out.print(\"Parent secret \"); }\n    public void reveal() { secret(); }\n}\nclass Child extends Parent {\n    private void secret() { System.out.print(\"Child secret \"); }\n    public static void main(String[] args) {\n        new Parent().reveal();\n    }\n}\n```"
    title_en: "Which private method is invoked in this inheritance scenario?\n```java\nclass Parent {\n    private void secret() { System.out.print(\"Parent secret \"); }\n    public void reveal() { secret(); }\n}\nclass Child extends Parent {\n    private void secret() { System.out.print(\"Child secret \"); }\n    public static void main(String[] args) {\n        new Parent().reveal();\n    }\n}\n```"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "Compilation error"
        text_en: "Compilation error"
      - label: "B"
        text_fr: "Parent secret Child secret"
        text_en: "Parent secret Child secret"
      - label: "C"
        text_fr: "Child secret"
        text_en: "Child secret"
      - label: "D"
        text_fr: "Parent secret"
        text_en: "Parent secret"
    correct_answers: ["D"]
    explanation_fr: "Private methods are not overridden. Parent.reveal() calls Parent.secret()."
    explanation_en: "Private methods are not overridden. Parent.reveal() calls Parent.secret()."
  - id: "jc-oop-004"
    title_fr: "Which order of constructor execution occurs in this inheritance example?\n```java\nclass Parent {\n    Parent() { System.out.print(\"P \"); }\n}\nclass Child extends Parent {\n    Child() { System.out.print(\"C \"); }\n}\nclass Family {\n    public static void main(String[] args) {\n        new Child();\n    }\n}\n```"
    title_en: "Which order of constructor execution occurs in this inheritance example?\n```java\nclass Parent {\n    Parent() { System.out.print(\"P \"); }\n}\nclass Child extends Parent {\n    Child() { System.out.print(\"C \"); }\n}\nclass Family {\n    public static void main(String[] args) {\n        new Child();\n    }\n}\n```"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "C P"
        text_en: "C P"
      - label: "B"
        text_fr: "P C"
        text_en: "P C"
      - label: "C"
        text_fr: "C only"
        text_en: "C only"
      - label: "D"
        text_fr: "Compilation error"
        text_en: "Compilation error"
    correct_answers: ["B"]
    explanation_fr: "Superclass constructor runs before subclass constructor."
    explanation_en: "Superclass constructor runs before subclass constructor."
  - id: "jc-oop-005"
    title_fr: "What is the effect of reassignment inside a method on a StringBuilder?\n```java\nclass Family {\n    static void test(StringBuilder sb) {\n        sb.append(\"Mom\");\n        sb = new StringBuilder(\"Dad\");\n    }\n    public static void main(String[] args) {\n        StringBuilder s = new StringBuilder(\"Son\");\n        test(s);\n        System.out.println(s);\n    }\n}\n```"
    title_en: "What is the effect of reassignment inside a method on a StringBuilder?\n```java\nclass Family {\n    static void test(StringBuilder sb) {\n        sb.append(\"Mom\");\n        sb = new StringBuilder(\"Dad\");\n    }\n    public static void main(String[] args) {\n        StringBuilder s = new StringBuilder(\"Son\");\n        test(s);\n        System.out.println(s);\n    }\n}\n```"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "Son"
        text_en: "Son"
      - label: "B"
        text_fr: "SonMom"
        text_en: "SonMom"
      - label: "C"
        text_fr: "Dad"
        text_en: "Dad"
      - label: "D"
        text_fr: "Compilation error"
        text_en: "Compilation error"
    correct_answers: ["B"]
    explanation_fr: "Append modifies the original. Reassignment inside the method has no effect on the caller."
    explanation_en: "Append modifies the original. Reassignment inside the method has no effect on the caller."
  - id: "jc-oop-006"
    title_fr: "How does equals behave with StringBuilder references?\n```java\nclass Family {\n    public static void main(String[] args) {\n        Object o = new StringBuilder(\"Dad\");\n        System.out.println(o.equals(new StringBuilder(\"Dad\")));\n    }\n}\n```"
    title_en: "How does equals behave with StringBuilder references?\n```java\nclass Family {\n    public static void main(String[] args) {\n        Object o = new StringBuilder(\"Dad\");\n        System.out.println(o.equals(new StringBuilder(\"Dad\")));\n    }\n}\n```"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "true"
        text_en: "true"
      - label: "B"
        text_fr: "false"
        text_en: "false"
      - label: "C"
        text_fr: "Compilation error"
        text_en: "Compilation error"
      - label: "D"
        text_fr: "Throws exception"
        text_en: "Throws exception"
    correct_answers: ["B"]
    explanation_fr: "StringBuilder does not override equals. Uses reference equality."
    explanation_en: "StringBuilder does not override equals. Uses reference equality."
  - id: "jc-oop-007"
    title_fr: "Inside a static method in Java 21, which of the following can be used to access an instance field?\n```java\nclass Family {\n    private String role = \"Parent\";\n    static void test() {\n        // ???\n    }\n}\n```"
    title_en: "Inside a static method in Java 21, which of the following can be used to access an instance field?\n```java\nclass Family {\n    private String role = \"Parent\";\n    static void test() {\n        // ???\n    }\n}\n```"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "Cannot access instance fields directly"
        text_en: "Cannot access instance fields directly"
      - label: "B"
        text_fr: "Access via creating an instance: new Family().role"
        text_en: "Access via creating an instance: new Family().role"
      - label: "C"
        text_fr: "Access via 'this.role'"
        text_en: "Access via 'this.role'"
      - label: "D"
        text_fr: "Access via static import"
        text_en: "Access via static import"
    correct_answers: ["B"]
    explanation_fr: "Static methods cannot access instance fields directly. To access an instance field, you need a reference to an object, e.g., 'new Family().role'. 'this' cannot be used in a static context, and static import does not help."
    explanation_en: "Static methods cannot access instance fields directly. To access an instance field, you need a reference to an object, e.g., 'new Family().role'. 'this' cannot be used in a static context, and static import does not help."
---

Java21DocCards - OOP questions.