---
id: "jc-streams"
title: "Java21DocCards - Streams"
lang: "en"
duration_minutes: 15
shuffle_questions: true
shuffle_options: true
difficulty: "medium"
tags: ["streams", "pipelines", "functional"]
questions:
  - id: "jc-streams-001"
    title_fr: "Which strings are selected after mapping and sorting this stream?\n```java\nimport java.util.*;\nimport java.util.stream.*;\nclass Family {\n  public static void main(String[] args) {\n    var members = List.of(\"Dad\",\"Mom\",\"Son\",\"Daughter\");\n    var result = members.stream()\n      .map(String::toUpperCase)\n      .sorted((a,b) -> b.compareTo(a))\n      .limit(2)\n      .collect(Collectors.joining(\", \"));\n    System.out.println(result);\n  }\n}\n```"
    title_en: "Which strings are selected after mapping and sorting this stream?\n```java\nimport java.util.*;\nimport java.util.stream.*;\nclass Family {\n  public static void main(String[] args) {\n    var members = List.of(\"Dad\",\"Mom\",\"Son\",\"Daughter\");\n    var result = members.stream()\n      .map(String::toUpperCase)\n      .sorted((a,b) -> b.compareTo(a))\n      .limit(2)\n      .collect(Collectors.joining(\", \"));\n    System.out.println(result);\n  }\n}\n```"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "SON, MOM"
        text_en: "SON, MOM"
      - label: "B"
        text_fr: "DAD, MOM"
        text_en: "DAD, MOM"
      - label: "C"
        text_fr: "SON, DAUGHTER"
        text_en: "SON, DAUGHTER"
      - label: "D"
        text_fr: "DAD, DAUGHTER"
        text_en: "DAD, DAUGHTER"
    correct_answers: ["A"]
    explanation_fr: "After mapping to uppercase and sorting descending, the first two elements are SON and MOM."
    explanation_en: "After mapping to uppercase and sorting descending, the first two elements are SON and MOM."
  - id: "jc-streams-002"
    title_fr: "Which elements are processed by dropWhile in this stream?\n```java\nimport java.util.*;\nclass Family {\n    public static void main(String[] args) {\n        List<String> list = List.of(\"Dad\",\"Mom\",\"Son\");\n        list.stream().dropWhile(s -> s.length() > 2).forEach(System.out::print);\n    }\n}\n```"
    title_en: "Which elements are processed by dropWhile in this stream?\n```java\nimport java.util.*;\nclass Family {\n    public static void main(String[] args) {\n        List<String> list = List.of(\"Dad\",\"Mom\",\"Son\");\n        list.stream().dropWhile(s -> s.length() > 2).forEach(System.out::print);\n    }\n}\n```"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "Son"
        text_en: "Son"
      - label: "B"
        text_fr: "Empty string"
        text_en: "Empty string"
      - label: "C"
        text_fr: "MomSon"
        text_en: "MomSon"
      - label: "D"
        text_fr: "DadMomSon"
        text_en: "DadMomSon"
    correct_answers: ["B"]
    explanation_fr: "dropWhile removes elements from the start while the predicate is true. Prints empty string"
    explanation_en: "dropWhile removes elements from the start while the predicate is true. Prints empty string"
  - id: "jc-streams-003"
    title_fr: "What is the structure of the Map returned by this family grouping operation?"
    title_en: "What is the structure of the Map returned by this family grouping operation?"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "1"
        text_en: "1"
      - label: "B"
        text_fr: "2"
        text_en: "2"
      - label: "C"
        text_fr: "3"
        text_en: "3"
      - label: "D"
        text_fr: "4"
        text_en: "4"
      - label: "E"
        text_fr: "Throws NullPointerException"
        text_en: "Throws NullPointerException"
    correct_answers: ["B"]
    explanation_fr: "partitioningBy creates 2 groups: age>=40 (true/false). Each partition is then grouped by isMarried status. result.get(true) gets the age>=40 partition containing [Dad, Mom]. Then .get(true) gets the married subgroup from that partition, which contains [Dad, Mom]. So size() returns 2. Note: Uncle is 38, so he's in the false partition."
    explanation_en: "partitioningBy creates 2 groups: age>=40 (true/false). Each partition is then grouped by isMarried status. result.get(true) gets the age>=40 partition containing [Dad, Mom]. Then .get(true) gets the married subgroup from that partition, which contains [Dad, Mom]. So size() returns 2. Note: Uncle is 38, so he's in the false partition."
  - id: "jc-streams-004"
    title_fr: "What is the output of this family genealogy processing code?\n```java\nimport java.util.*;\nimport java.util.stream.*;\nclass Family {\n    public static void main(String[] args) {\n        List<String> genealogy = List.of(\"Abraham-Sara\", \"Adam\");\n        \n        String result = genealogy.stream()\n            .flatMap(data -> Arrays.stream(data.split(\"-\")))\n            .filter(part -> part.length() >= 4)\n            .map(part -> part.length() == 4 ? part.substring(1) : part.substring(1, 3))\n            .reduce(\"\", (a, b) -> a + b.toUpperCase());\n        \n        System.out.println(result);\n    }\n}\n```"
    title_en: "What is the output of this family genealogy processing code?\n```java\nimport java.util.*;\nimport java.util.stream.*;\nclass Family {\n    public static void main(String[] args) {\n        List<String> genealogy = List.of(\"Abraham-Sara\", \"Adam\");\n        \n        String result = genealogy.stream()\n            .flatMap(data -> Arrays.stream(data.split(\"-\")))\n            .filter(part -> part.length() >= 4)\n            .map(part -> part.length() == 4 ? part.substring(1) : part.substring(1, 3))\n            .reduce(\"\", (a, b) -> a + b.toUpperCase());\n        \n        System.out.println(result);\n    }\n}\n```"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "BRARADAM"
        text_en: "BRARADAM"
      - label: "B"
        text_fr: "BRAHAMSARA"
        text_en: "BRAHAMSARA"
      - label: "C"
        text_fr: "ABRASA"
        text_en: "ABRASA"
      - label: "D"
        text_fr: "ABARSADAM"
        text_en: "ABARSADAM"
      - label: "E"
        text_fr: "BRARDA"
        text_en: "BRARDA"
      - label: "F"
        text_fr: "ABSADAM"
        text_en: "ABSADAM"
    correct_answers: ["A"]
    explanation_fr: "flatMap splits by '-' and flattens: ['Abraham','Sara','Adam']. filter(length >= 4) keeps all: ['Abraham','Sara','Adam']. map applies substring rules: Abraham(7chars)->substring(1,3)->'br', Sara(4chars)->substring(1)->'ara', Adam(4chars)->substring(1)->'dam'. reduce concatenates with toUpperCase: 'BR'+'ARA'+'DAM' = 'BRARADAM'."
    explanation_en: "flatMap splits by '-' and flattens: ['Abraham','Sara','Adam']. filter(length >= 4) keeps all: ['Abraham','Sara','Adam']. map applies substring rules: Abraham(7chars)->substring(1,3)->'br', Sara(4chars)->substring(1)->'ara', Adam(4chars)->substring(1)->'dam'. reduce concatenates with toUpperCase: 'BR'+'ARA'+'DAM' = 'BRARADAM'."
---

Java21DocCards - Streams questions.