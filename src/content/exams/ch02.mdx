---
id: "ch02"
title: "Chapter 2 - Utilizing Java Object-Oriented Approach - Part 2"
lang: "en"
duration_minutes: 32
shuffle_questions: true
shuffle_options: true
difficulty: "medium"
tags: ["oop", "inheritance", "polymorphism", "interfaces"]
questions:
  - id: "ch02-001"
    title_fr: "What is the result of compiling and executing the following code?\n```java\nvoid myMethod() {\nint x = 1;\nif (x > 0) {\nint y = 2;\nSystem.out.println(x + y);\n}\nSystem.out.println(x);\nSystem.out.println(y);\n}\n```"
    title_en: "What is the result of compiling and executing the following code?\n```java\nvoid myMethod() {\nint x = 1;\nif (x > 0) {\nint y = 2;\nSystem.out.println(x + y);\n}\nSystem.out.println(x);\nSystem.out.println(y);\n}\n```"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "The code compiles and outputs `3` followed by `1`."
        text_en: "The code compiles and outputs `3` followed by `1`."
      - label: "B"
        text_fr: "The code compiles and outputs `3` followed by `1` and an undefined value for `y`."
        text_en: "The code compiles and outputs `3` followed by `1` and an undefined value for `y`."
      - label: "C"
        text_fr: "The code does not compile because `y` is accessed outside of its scope."
        text_en: "The code does not compile because `y` is accessed outside of its scope."
      - label: "D"
        text_fr: "The code compiles but throws a runtime exception when trying to print `y`."
        text_en: "The code compiles but throws a runtime exception when trying to print `y`."
    correct_answers: ["C"]
    explanation_fr: "A) Incorrect: because, although the code prints `3` followed by `1` due to `x` being in scope, attempting to access `y` outside of its declaring block (the `if` block) will cause a compile-time error. B) Incorrect: because Java does not allow access to local variables (`y` in this case) outside of their scope. The notion of an \"undefined value for `y`\" is not applicable here; the compiler will simply not compile the code. C) Correct: option. Local variable `y` is declared inside the `if` block, and thus, it is only accessible within that block. Trying to access it outside of its scope, as done in the last `System.out.println(y);`, causes a compile-time error, specifically saying that `y` cannot be found. D) Incorrect: because the issue with the code is at compile time, not runtime. The compiler will not allow the code to compile due to the scope violation of the local variable `y`, hence a runtime exception regarding `y` is out of the question."
    explanation_en: "A) Incorrect: because, although the code prints `3` followed by `1` due to `x` being in scope, attempting to access `y` outside of its declaring block (the `if` block) will cause a compile-time error. B) Incorrect: because Java does not allow access to local variables (`y` in this case) outside of their scope. The notion of an \"undefined value for `y`\" is not applicable here; the compiler will simply not compile the code. C) Correct: option. Local variable `y` is declared inside the `if` block, and thus, it is only accessible within that block. Trying to access it outside of its scope, as done in the last `System.out.println(y);`, causes a compile-time error, specifically saying that `y` cannot be found. D) Incorrect: because the issue with the code is at compile time, not runtime. The compiler will not allow the code to compile due to the scope violation of the local variable `y`, hence a runtime exception regarding `y` is out of the question."
  - id: "ch02-002"
    title_fr: "Which of the following variable declarations statements are valid? (Choose all that apply.)"
    title_en: "Which of the following variable declarations statements are valid? (Choose all that apply.)"
    type: "multiple-choice"
    options:
      - label: "A"
        text_fr: "`double x, double y;`"
        text_en: "`double x, double y;`"
      - label: "B"
        text_fr: "`int i = 0, String s = \"hello\";`"
        text_en: "`int i = 0, String s = \"hello\";`"
      - label: "C"
        text_fr: "`float f1 = 3.14f, f2 = 6.28f;`"
        text_en: "`float f1 = 3.14f, f2 = 6.28f;`"
      - label: "D"
        text_fr: "`char a = 'A', b, c = 'C';`"
        text_en: "`char a = 'A', b, c = 'C';`"
    correct_answers: ["C", "D"]
    explanation_fr: "A) Incorrect: because when declaring multiple variables of the same type in a single statement, you do not repeat the type before each variable. The correct syntax would be `double x, y;`. B) Incorrect: for the same reason as A; you cannot declare variables of different types (`int` and `String` in this case) in a single statement. C) Correct: This is correct. You can declare multiple variables of the same type (`float` in this case) in a single statement, and it's also fine to initialize them with values in the same statement. D) Correct: This is correct. It's valid to declare multiple variables of the same type (`char` in this case), and initialize some, all, or none of them in the same statement."
    explanation_en: "A) Incorrect: because when declaring multiple variables of the same type in a single statement, you do not repeat the type before each variable. The correct syntax would be `double x, y;`. B) Incorrect: for the same reason as A; you cannot declare variables of different types (`int` and `String` in this case) in a single statement. C) Correct: This is correct. You can declare multiple variables of the same type (`float` in this case) in a single statement, and it's also fine to initialize them with values in the same statement. D) Correct: This is correct. It's valid to declare multiple variables of the same type (`char` in this case), and initialize some, all, or none of them in the same statement."
  - id: "ch02-003"
    title_fr: "Which of the following statements are true regarding the use of `var` in Java? (Choose all that apply.)"
    title_en: "Which of the following statements are true regarding the use of `var` in Java? (Choose all that apply.)"
    type: "multiple-choice"
    options:
      - label: "A"
        text_fr: "`var` can be used to declare both local variables within methods and instance variables within classes."
        text_en: "`var` can be used to declare both local variables within methods and instance variables within classes."
      - label: "B"
        text_fr: "The use of `var` is restricted to local variables within methods, constructors, or initializer blocks."
        text_en: "The use of `var` is restricted to local variables within methods, constructors, or initializer blocks."
      - label: "C"
        text_fr: "`var` can be used to declare method parameters."
        text_en: "`var` can be used to declare method parameters."
      - label: "D"
        text_fr: "`var` enhances readability by inferring types where it's clear from the context, but it's not allowed in method signatures to maintain clarity."
        text_en: "`var` enhances readability by inferring types where it's clear from the context, but it's not allowed in method signatures to maintain clarity."
      - label: "E"
        text_fr: "`var` can be used to declare class (static) variables."
        text_en: "`var` can be used to declare class (static) variables."
    correct_answers: ["B", "D"]
    explanation_fr: "A) Incorrect: because `var` cannot be used to declare instance variables. It is specifically restricted to local variables within methods, constructors, or initializer blocks, as using `var` for fields would reduce the clarity of a class's public API. B) Correct: `var` is intended for local variable type inference, significantly reducing the verbosity of Java code in scenarios where the compiler can easily determine the type of the local variable from its initializer. Its use is restricted to ensure clarity and prevent ambiguity in more complex constructs like class fields or method parameters. C) Incorrect: The example clearly demonstrates that `var` cannot be used to declare method parameters. This limitation ensures that method signatures remain explicit in their type requirements, a critical aspect of a class's contract with its callers. D) Correct: While `var` is primarily used to improve code readability by reducing the need for explicit type declarations where the type can be inferred from the context, it is not allowed in method signatures. This restriction ensures that the types of parameters in methods are always explicitly defined, aiding in the readability and maintainability of public APIs. E) Incorrect: Similar to instance variables, `var` is not permissible for declaring class (`static`) variables. The rationale behind this restriction aligns with the goal of maintaining explicit type declarations in the class's structure, ensuring the class's design remains clear and unambiguous to both the compiler and developers."
    explanation_en: "A) Incorrect: because `var` cannot be used to declare instance variables. It is specifically restricted to local variables within methods, constructors, or initializer blocks, as using `var` for fields would reduce the clarity of a class's public API. B) Correct: `var` is intended for local variable type inference, significantly reducing the verbosity of Java code in scenarios where the compiler can easily determine the type of the local variable from its initializer. Its use is restricted to ensure clarity and prevent ambiguity in more complex constructs like class fields or method parameters. C) Incorrect: The example clearly demonstrates that `var` cannot be used to declare method parameters. This limitation ensures that method signatures remain explicit in their type requirements, a critical aspect of a class's contract with its callers. D) Correct: While `var` is primarily used to improve code readability by reducing the need for explicit type declarations where the type can be inferred from the context, it is not allowed in method signatures. This restriction ensures that the types of parameters in methods are always explicitly defined, aiding in the readability and maintainability of public APIs. E) Incorrect: Similar to instance variables, `var` is not permissible for declaring class (`static`) variables. The rationale behind this restriction aligns with the goal of maintaining explicit type declarations in the class's structure, ensuring the class's design remains clear and unambiguous to both the compiler and developers."
  - id: "ch02-004"
    title_fr: "Which of the following statements correctly describe the use of inheritance in Java? (Choose all that apply.)"
    title_en: "Which of the following statements correctly describe the use of inheritance in Java? (Choose all that apply.)"
    type: "multiple-choice"
    options:
      - label: "A"
        text_fr: "Subclasses can only access `protected` and `public` members of their superclass directly."
        text_en: "Subclasses can only access `protected` and `public` members of their superclass directly."
      - label: "B"
        text_fr: "In Java, a class can extend multiple classes to achieve multiple inheritance."
        text_en: "In Java, a class can extend multiple classes to achieve multiple inheritance."
      - label: "C"
        text_fr: "The `extends` keyword is used in Java to create a subclass that inherits from a superclass."
        text_en: "The `extends` keyword is used in Java to create a subclass that inherits from a superclass."
      - label: "D"
        text_fr: "A subclass in Java can directly access `private` members of its superclass."
        text_en: "A subclass in Java can directly access `private` members of its superclass."
    correct_answers: ["A", "C"]
    explanation_fr: "A) Correct: Subclasses can directly access `protected` and `public` members of their superclass. This accessibility allows subclasses to leverage and extend the functionality provided by the superclass while maintaining encapsulation of `private` members. B) Incorrect: Java does not support multiple inheritance for classes. A class in Java can only extend one other class, preventing complications like the diamond problem and the complexity associated with multiple inheritance. C) Correct: The `extends` keyword is indeed used to define a subclass that inherits properties and behaviors from a single superclass, establishing an *is-a* relationship between the subclass and the superclass. This is a fundamental concept in Java's implementation of inheritance. D) Incorrect: A subclass cannot directly access `private` members of its superclass. Instead, it can access them through `public` or `protected` accessors provided by the superclass. This encapsulation principle ensures a controlled interaction with the superclass's state."
    explanation_en: "A) Correct: Subclasses can directly access `protected` and `public` members of their superclass. This accessibility allows subclasses to leverage and extend the functionality provided by the superclass while maintaining encapsulation of `private` members. B) Incorrect: Java does not support multiple inheritance for classes. A class in Java can only extend one other class, preventing complications like the diamond problem and the complexity associated with multiple inheritance. C) Correct: The `extends` keyword is indeed used to define a subclass that inherits properties and behaviors from a single superclass, establishing an *is-a* relationship between the subclass and the superclass. This is a fundamental concept in Java's implementation of inheritance. D) Incorrect: A subclass cannot directly access `private` members of its superclass. Instead, it can access them through `public` or `protected` accessors provided by the superclass. This encapsulation principle ensures a controlled interaction with the superclass's state."
  - id: "ch02-005"
    title_fr: "Consider the following code snippet:\n```java\nabstract class Animal {\nabstract void eat();\n}\nclass Dog extends Animal {\nvoid eat() {\nSystem.out.println(\"Dog eats\");\n}\n}\nclass Cat extends Animal {\nvoid eat() {\nSystem.out.println(\"Cat eats\");\n}\n}\npublic class Test {\npublic static void main(String[] args) {\nAnimal myAnimal = new Dog();\nmyAnimal.eat();\n}\n}\n```\nWhich of the following statements is true regarding the above code? Choose all that apply."
    title_en: "Consider the following code snippet:\n```java\nabstract class Animal {\nabstract void eat();\n}\nclass Dog extends Animal {\nvoid eat() {\nSystem.out.println(\"Dog eats\");\n}\n}\nclass Cat extends Animal {\nvoid eat() {\nSystem.out.println(\"Cat eats\");\n}\n}\npublic class Test {\npublic static void main(String[] args) {\nAnimal myAnimal = new Dog();\nmyAnimal.eat();\n}\n}\n```\nWhich of the following statements is true regarding the above code? Choose all that apply."
    type: "multiple-choice"
    options:
      - label: "A"
        text_fr: "The code will compile and print `\"Dog eats\"` when executed."
        text_en: "The code will compile and print `\"Dog eats\"` when executed."
      - label: "B"
        text_fr: "The `Animal` class can be instantiated."
        text_en: "The `Animal` class can be instantiated."
      - label: "C"
        text_fr: "Removing the `eat` method from the `Dog` class will cause a compilation error."
        text_en: "Removing the `eat` method from the `Dog` class will cause a compilation error."
      - label: "D"
        text_fr: "The `Cat` class is necessary for the code to compile and run."
        text_en: "The `Cat` class is necessary for the code to compile and run."
    correct_answers: ["A", "C"]
    explanation_fr: "A) Correct: The `Dog` class has provided an implementation for the `eat` method, which is abstract in the superclass `Animal`. Since `myAnimal` is of type `Animal` but instantiated as a `Dog`, it will call the overridden `eat` method in the `Dog` class, printing `\"Dog eats\"`. B) Incorrect: The `Animal` class is abstract and cannot be instantiated. Attempting to create an instance of `Animal` directly (`new Animal()`) would result in a compilation error. C) Correct: Since `Dog` extends the abstract class `Animal` and `Animal` has an abstract `eat` method, `Dog` must provide an implementation for `eat`. Failing to do so will prevent the code from compiling because `Dog` would also be considered abstract. D) Incorrect: The `Cat` class is not referenced in the `main` method or anywhere else in the provided code snippet. Thus, it is unnecessary for the compilation and execution of the given code segment."
    explanation_en: "A) Correct: The `Dog` class has provided an implementation for the `eat` method, which is abstract in the superclass `Animal`. Since `myAnimal` is of type `Animal` but instantiated as a `Dog`, it will call the overridden `eat` method in the `Dog` class, printing `\"Dog eats\"`. B) Incorrect: The `Animal` class is abstract and cannot be instantiated. Attempting to create an instance of `Animal` directly (`new Animal()`) would result in a compilation error. C) Correct: Since `Dog` extends the abstract class `Animal` and `Animal` has an abstract `eat` method, `Dog` must provide an implementation for `eat`. Failing to do so will prevent the code from compiling because `Dog` would also be considered abstract. D) Incorrect: The `Cat` class is not referenced in the `main` method or anywhere else in the provided code snippet. Thus, it is unnecessary for the compilation and execution of the given code segment."
  - id: "ch02-006"
    title_fr: "Consider the following interfaces:\n```java\ninterface Walkable {\nint distance = 10;\nvoid walk();\n}\ninterface Runnable {\nvoid run();\ndefault void getSpeed() {\nSystem.out.println(\"Default speed\");\n}\n}\nclass Person implements Walkable, Runnable {\npublic void walk() {\nSystem.out.println(\"Walking...\");\n}\npublic void run() {\nSystem.out.println(\"Running...\");\n}\n}\n```\nWhich of the following statements is true?"
    title_en: "Consider the following interfaces:\n```java\ninterface Walkable {\nint distance = 10;\nvoid walk();\n}\ninterface Runnable {\nvoid run();\ndefault void getSpeed() {\nSystem.out.println(\"Default speed\");\n}\n}\nclass Person implements Walkable, Runnable {\npublic void walk() {\nSystem.out.println(\"Walking...\");\n}\npublic void run() {\nSystem.out.println(\"Running...\");\n}\n}\n```\nWhich of the following statements is true?"
    type: "multiple-choice"
    options:
      - label: "A"
        text_fr: "The `Person` class must override the `getSpeed` method."
        text_en: "The `Person` class must override the `getSpeed` method."
      - label: "B"
        text_fr: "The `distance` variable in the `Walkable` interface is implicitly `public`, `static`, and `final`."
        text_en: "The `distance` variable in the `Walkable` interface is implicitly `public`, `static`, and `final`."
      - label: "C"
        text_fr: "A `Person` object can call the `getSpeed` method without any implementation in the `Person` class."
        text_en: "A `Person` object can call the `getSpeed` method without any implementation in the `Person` class."
      - label: "D"
        text_fr: "The `Runnable` interface causes a compilation error due to a naming conflict with `java.lang.Runnable`."
        text_en: "The `Runnable` interface causes a compilation error due to a naming conflict with `java.lang.Runnable`."
    correct_answers: ["B", "C"]
    explanation_fr: "A) Incorrect: The `Person` class is not required to override the `getSpeed` method because it is a default method in the `Runnable` interface. Default methods provide an implementation that can be used or overridden by implementing classes, but overriding is not mandatory. B) Correct: In Java, all variables declared in an interface are implicitly `public`, `static`, and `final`. This means the `distance` variable in the `Walkable` interface is a constant and must be initialized at the point of declaration. It is accessible with the interface name, like `Walkable.distance`. C) Correct: Since the `Runnable` interface provides a default implementation for the `getSpeed` method, a `Person` object can call the `getSpeed` method without any additional implementation in the `Person` class itself. The default implementation from the interface will be used. D) Incorrect: because Java fully supports namespace resolution. The `Runnable` interface declared in the code snippet and `java.lang.Runnable` exist in different packages. There is no compilation error unless there's an attempt to import both in the same file without using a fully qualified name. Plus, this situation does not directly relate to the functionality or declaration of interfaces per the exam's focus."
    explanation_en: "A) Incorrect: The `Person` class is not required to override the `getSpeed` method because it is a default method in the `Runnable` interface. Default methods provide an implementation that can be used or overridden by implementing classes, but overriding is not mandatory. B) Correct: In Java, all variables declared in an interface are implicitly `public`, `static`, and `final`. This means the `distance` variable in the `Walkable` interface is a constant and must be initialized at the point of declaration. It is accessible with the interface name, like `Walkable.distance`. C) Correct: Since the `Runnable` interface provides a default implementation for the `getSpeed` method, a `Person` object can call the `getSpeed` method without any additional implementation in the `Person` class itself. The default implementation from the interface will be used. D) Incorrect: because Java fully supports namespace resolution. The `Runnable` interface declared in the code snippet and `java.lang.Runnable` exist in different packages. There is no compilation error unless there's an attempt to import both in the same file without using a fully qualified name. Plus, this situation does not directly relate to the functionality or declaration of interfaces per the exam's focus."
  - id: "ch02-007"
    title_fr: "Consider the following code snippet related to sealed classes:\n```java\nsealed abstract class Shape permits Circle, Square {\nabstract double area();\n}\nfinal class Circle extends Shape {\nprivate final double radius;\nCircle(double radius) {\nthis.radius = radius;\n}\npublic double area() {\nreturn Math.PI * radius * radius;\n}\n}\nnon-sealed class Square extends Shape {\nprivate final double side;\nSquare(double side) {\nthis.side = side;\n}\npublic double area() {\nreturn side * side;\n}\n}\npublic class TestShapes {\npublic static void main(String[] args) {\nShape shape = new Circle(10);\nSystem.out.println(\"Area: \" + shape.area());\n}\n}\n```\nWhich of the following statements is true?"
    title_en: "Consider the following code snippet related to sealed classes:\n```java\nsealed abstract class Shape permits Circle, Square {\nabstract double area();\n}\nfinal class Circle extends Shape {\nprivate final double radius;\nCircle(double radius) {\nthis.radius = radius;\n}\npublic double area() {\nreturn Math.PI * radius * radius;\n}\n}\nnon-sealed class Square extends Shape {\nprivate final double side;\nSquare(double side) {\nthis.side = side;\n}\npublic double area() {\nreturn side * side;\n}\n}\npublic class TestShapes {\npublic static void main(String[] args) {\nShape shape = new Circle(10);\nSystem.out.println(\"Area: \" + shape.area());\n}\n}\n```\nWhich of the following statements is true?"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "The `Shape` class is correctly defined as a sealed class, allowing only specified classes to extend it."
        text_en: "The `Shape` class is correctly defined as a sealed class, allowing only specified classes to extend it."
      - label: "B"
        text_fr: "The `Square` class does not correctly extend the `Shape` class because it is not marked as `final`."
        text_en: "The `Square` class does not correctly extend the `Shape` class because it is not marked as `final`."
      - label: "C"
        text_fr: "The `Circle` class can be further extended by other classes."
        text_en: "The `Circle` class can be further extended by other classes."
      - label: "D"
        text_fr: "The `area` method in the `Shape` class must provide a default implementation."
        text_en: "The `area` method in the `Shape` class must provide a default implementation."
    correct_answers: ["A"]
    explanation_fr: "A) Correct: The `Shape` class is declared as a sealed class, which means it can be extended only by the classes it explicitly permits through the `permits` clause. In this case, `Shape` permits `Circle` and `Square` to extend it, and both classes are correctly defined as permitted subclasses. B) Incorrect: There's no requirement for classes extending a sealed class to be marked as `final` if they are non-sealed. The keyword `non-sealed` explicitly allows the `Square` class to extend the sealed `Shape` class without being final, indicating it can be further extended. C) Incorrect: The `Circle` class is declared as `final`, which means it cannot be extended further and aligning with the constraints of extending a sealed class where the permitted subclass can be final, sealed, or non-sealed. D) Incorrect: Abstract classes like `Shape` are not required to provide implementations for their abstract methods. The purpose of an abstract class is to define a template that its subclasses will follow, which includes implementing any abstract methods declared in the abstract class."
    explanation_en: "A) Correct: The `Shape` class is declared as a sealed class, which means it can be extended only by the classes it explicitly permits through the `permits` clause. In this case, `Shape` permits `Circle` and `Square` to extend it, and both classes are correctly defined as permitted subclasses. B) Incorrect: There's no requirement for classes extending a sealed class to be marked as `final` if they are non-sealed. The keyword `non-sealed` explicitly allows the `Square` class to extend the sealed `Shape` class without being final, indicating it can be further extended. C) Incorrect: The `Circle` class is declared as `final`, which means it cannot be extended further and aligning with the constraints of extending a sealed class where the permitted subclass can be final, sealed, or non-sealed. D) Incorrect: Abstract classes like `Shape` are not required to provide implementations for their abstract methods. The purpose of an abstract class is to define a template that its subclasses will follow, which includes implementing any abstract methods declared in the abstract class."
  - id: "ch02-008"
    title_fr: "Consider the following class:\n```java\npublic class Widget {\nprivate int size;\npublic Widget() {\nthis(10); // Line 5\n}\npublic Widget(int size) {\nthis.size = size;\n}\npublic void resize(int size) {\nif (size > this.size) {\nthis.size = size; // Line 14\nupdateWidget();\n}\n}\nprivate void updateWidget() {\nSystem.out.println(\"Widget updated to size \" + this.size);\n}\npublic static void main(String[] args) {\nWidget widget = new Widget();\nwidget.resize(15);\n}\n}\n```\nIn line 14, what does the `this` keyword represent in the context of the `Widget` class?"
    title_en: "Consider the following class:\n```java\npublic class Widget {\nprivate int size;\npublic Widget() {\nthis(10); // Line 5\n}\npublic Widget(int size) {\nthis.size = size;\n}\npublic void resize(int size) {\nif (size > this.size) {\nthis.size = size; // Line 14\nupdateWidget();\n}\n}\nprivate void updateWidget() {\nSystem.out.println(\"Widget updated to size \" + this.size);\n}\npublic static void main(String[] args) {\nWidget widget = new Widget();\nwidget.resize(15);\n}\n}\n```\nIn line 14, what does the `this` keyword represent in the context of the `Widget` class?"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "A reference to the `static` context of the class, allowing access to static methods and fields."
        text_en: "A reference to the `static` context of the class, allowing access to static methods and fields."
      - label: "B"
        text_fr: "A special variable that stores the return value of a method."
        text_en: "A special variable that stores the return value of a method."
      - label: "C"
        text_fr: "An optional keyword that can always be omitted without affecting the functionality of the code."
        text_en: "An optional keyword that can always be omitted without affecting the functionality of the code."
      - label: "D"
        text_fr: "A reference to the current object, whose instance variable is being called."
        text_en: "A reference to the current object, whose instance variable is being called."
    correct_answers: ["D"]
    explanation_fr: "A) Incorrect: The `this` keyword does not refer to the static context of the class. It specifically refers to the current instance of the class. Static methods and fields belong to the class itself and are not part of any instance, so they cannot be accessed through `this`. B) Incorrect: The `this` keyword does not store the return value of a method. It is used within an instance method or a constructor to refer to the current object the method or constructor is being invoked upon. C) Incorrect: While it is true that in some cases `this` can be omitted (for example, when accessing instance fields or methods without any naming conflict), its use is necessary for situations like constructor chaining (`this()` call) or when the method parameter names shadow the instance field names. In such scenarios, `this` clarifies to which variable the code is referring. D) Correct: The `this` keyword in Java is used to refer to the current object—the object whose instance variable, method, or constructor is being called. You can see its usage in line 5 to call another constructor within the same class, in line 14 to differentiate between the method parameter `size` and the instance variable `size`, and in the `updateWidget` method to access the instance variable `size`. This usage demonstrates `this` as a way to refer explicitly to properties or methods of the current object."
    explanation_en: "A) Incorrect: The `this` keyword does not refer to the static context of the class. It specifically refers to the current instance of the class. Static methods and fields belong to the class itself and are not part of any instance, so they cannot be accessed through `this`. B) Incorrect: The `this` keyword does not store the return value of a method. It is used within an instance method or a constructor to refer to the current object the method or constructor is being invoked upon. C) Incorrect: While it is true that in some cases `this` can be omitted (for example, when accessing instance fields or methods without any naming conflict), its use is necessary for situations like constructor chaining (`this()` call) or when the method parameter names shadow the instance field names. In such scenarios, `this` clarifies to which variable the code is referring. D) Correct: The `this` keyword in Java is used to refer to the current object—the object whose instance variable, method, or constructor is being called. You can see its usage in line 5 to call another constructor within the same class, in line 14 to differentiate between the method parameter `size` and the instance variable `size`, and in the `updateWidget` method to access the instance variable `size`. This usage demonstrates `this` as a way to refer explicitly to properties or methods of the current object."
  - id: "ch02-009"
    title_fr: "Consider the following classes:\n```java\nclass Animal {\nString name;\nAnimal(String name) {\nthis.name = name;\n}\nprotected void eat() {\nSystem.out.println(\"Animal eats\");\n}\n}\nclass Dog extends Animal {\nDog(String name) {\nsuper(name);\n}\n@Override\nprotected void eat() {\nsuper.eat();\nSystem.out.println(name + \" (Dog) eats\");\n}\n}\npublic class TestAnimal {\npublic static void main(String[] args) {\nAnimal myDog = new Dog(\"Buddy\");\nmyDog.eat();\n}\n}\n```\nWhich of the following statements are true regarding the use of `super` in the above code? (Choose all that apply.)"
    title_en: "Consider the following classes:\n```java\nclass Animal {\nString name;\nAnimal(String name) {\nthis.name = name;\n}\nprotected void eat() {\nSystem.out.println(\"Animal eats\");\n}\n}\nclass Dog extends Animal {\nDog(String name) {\nsuper(name);\n}\n@Override\nprotected void eat() {\nsuper.eat();\nSystem.out.println(name + \" (Dog) eats\");\n}\n}\npublic class TestAnimal {\npublic static void main(String[] args) {\nAnimal myDog = new Dog(\"Buddy\");\nmyDog.eat();\n}\n}\n```\nWhich of the following statements are true regarding the use of `super` in the above code? (Choose all that apply.)"
    type: "multiple-choice"
    options:
      - label: "A"
        text_fr: "The `super` keyword is used in the `Dog` constructor to call the superclass constructor."
        text_en: "The `super` keyword is used in the `Dog` constructor to call the superclass constructor."
      - label: "B"
        text_fr: "The `eat` method in the `Dog` class uses `super` to invoke the superclass's `eat` method."
        text_en: "The `eat` method in the `Dog` class uses `super` to invoke the superclass's `eat` method."
      - label: "C"
        text_fr: "Removing the `super.eat();` call in the `Dog` class's `eat` method will prevent the `Dog` class from compiling."
        text_en: "Removing the `super.eat();` call in the `Dog` class's `eat` method will prevent the `Dog` class from compiling."
      - label: "D"
        text_fr: "The `super` keyword can be used to access `static` methods from the superclass."
        text_en: "The `super` keyword can be used to access `static` methods from the superclass."
    correct_answers: ["A", "B"]
    explanation_fr: "A) Correct: This optional is correct. In the `Dog` constructor, `super(name);` is used to call the superclass (`Animal`) constructor with the `name` parameter. This is necessary to initialize the `name` field inherited from the `Animal` class in the `Dog` instance. B) Correct: This optional is correct. The `eat` method in the `Dog` class calls `super.eat();` to invoke the `eat` method defined in the superclass (`Animal`). This allows the `Dog` class to extend the functionality of the `eat` method beyond what is defined in the superclass, demonstrating method overriding and use of `super` to access the overridden method. C) Incorrect: This optional is incorrect. Removing the `super.eat();` call from the `Dog` class's `eat` method would not prevent the class from compiling. It would simply mean that the `Dog` class's `eat` method no longer calls the superclass's `eat` method, altering the program's behavior but not its compilability. D) Incorrect: This optional is incorrect. While `super` can indeed be used to access superclass methods, it's not specifically used or necessary for accessing static methods. Static methods belong to the class, not to instances, and should be invoked using the class name. `super` is used primarily for instance methods and constructors."
    explanation_en: "A) Correct: This optional is correct. In the `Dog` constructor, `super(name);` is used to call the superclass (`Animal`) constructor with the `name` parameter. This is necessary to initialize the `name` field inherited from the `Animal` class in the `Dog` instance. B) Correct: This optional is correct. The `eat` method in the `Dog` class calls `super.eat();` to invoke the `eat` method defined in the superclass (`Animal`). This allows the `Dog` class to extend the functionality of the `eat` method beyond what is defined in the superclass, demonstrating method overriding and use of `super` to access the overridden method. C) Incorrect: This optional is incorrect. Removing the `super.eat();` call from the `Dog` class's `eat` method would not prevent the class from compiling. It would simply mean that the `Dog` class's `eat` method no longer calls the superclass's `eat` method, altering the program's behavior but not its compilability. D) Incorrect: This optional is incorrect. While `super` can indeed be used to access superclass methods, it's not specifically used or necessary for accessing static methods. Static methods belong to the class, not to instances, and should be invoked using the class name. `super` is used primarily for instance methods and constructors."
  - id: "ch02-010"
    title_fr: "Consider the following classes:\n```java\nclass Vehicle {\npublic void drive(int speed) {\nSystem.out.println(\"Vehicle driving at speed: \" + speed);\n}\n}\nclass Car extends Vehicle {\n@Override\npublic void drive(long speed) {\nSystem.out.println(\"Car driving at speed: \" + speed);\n}\n}\npublic class TestDrive {\npublic static void main(String[] args) {\nVehicle myCar = new Car();\nmyCar.drive(60);\n}\n}\n```\nWhat is the result of compiling and executing the above code?"
    title_en: "Consider the following classes:\n```java\nclass Vehicle {\npublic void drive(int speed) {\nSystem.out.println(\"Vehicle driving at speed: \" + speed);\n}\n}\nclass Car extends Vehicle {\n@Override\npublic void drive(long speed) {\nSystem.out.println(\"Car driving at speed: \" + speed);\n}\n}\npublic class TestDrive {\npublic static void main(String[] args) {\nVehicle myCar = new Car();\nmyCar.drive(60);\n}\n}\n```\nWhat is the result of compiling and executing the above code?"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "It compiles and prints `\"Car driving at speed: 60\"`."
        text_en: "It compiles and prints `\"Car driving at speed: 60\"`."
      - label: "B"
        text_fr: "It does not compile because the `drive` method cannot be called using a `Vehicle` reference."
        text_en: "It does not compile because the `drive` method cannot be called using a `Vehicle` reference."
      - label: "C"
        text_fr: "It does not compile because the `drive` method in the `Car` class does not properly override the `drive` method in the `Vehicle` class."
        text_en: "It does not compile because the `drive` method in the `Car` class does not properly override the `drive` method in the `Vehicle` class."
      - label: "D"
        text_fr: "It compiles and prints `\"Vehicle driving at speed: 60\"` because the `drive` method in the `Car` class is an overload, not an override."
        text_en: "It compiles and prints `\"Vehicle driving at speed: 60\"` because the `drive` method in the `Car` class is an overload, not an override."
    correct_answers: ["C"]
    explanation_fr: "A) Incorrect: because the `drive` method in the `Car` class has a different parameter type (`long`) than the method in the `Vehicle` class (`int`). Due to the difference in parameter types, the `Car` class's `drive` method does not override but rather overloads the `Vehicle` class's `drive` method. Since the method is called on a `Vehicle` reference, the `Vehicle` class's `drive` method is invoked. B) Incorrect: because `Vehicle` defines the `drive` method correctly. C) Correct: The `@Override` annotation explicitly tells the compiler that the annotated method is intended to override a method from a superclass. However, the `drive` method in the `Car` class has a parameter of type `long`, while the `drive` method in the `Vehicle` class has a parameter of type `int`. Since the parameter types differ, the `Car` method does not override the `Vehicle` method, it's an overload. The compiler enforces the `@Override` contract strictly, so it produces an error such as: `method does not override or implement a method from a supertype`. As a result, the code fails to compile. D) Incorrect: It correctly identifies that the `Car` method is an overload rather than an override, but it incorrectly concludes that the code compiles. The `@Override` annotation prevents compilation because the annotated method is not actually overriding anything (see option C). If the `@Override` annotation were removed, then this option would describe the correct behavior. But with the annotation present, the code does not compile."
    explanation_en: "A) Incorrect: because the `drive` method in the `Car` class has a different parameter type (`long`) than the method in the `Vehicle` class (`int`). Due to the difference in parameter types, the `Car` class's `drive` method does not override but rather overloads the `Vehicle` class's `drive` method. Since the method is called on a `Vehicle` reference, the `Vehicle` class's `drive` method is invoked. B) Incorrect: because `Vehicle` defines the `drive` method correctly. C) Correct: The `@Override` annotation explicitly tells the compiler that the annotated method is intended to override a method from a superclass. However, the `drive` method in the `Car` class has a parameter of type `long`, while the `drive` method in the `Vehicle` class has a parameter of type `int`. Since the parameter types differ, the `Car` method does not override the `Vehicle` method, it's an overload. The compiler enforces the `@Override` contract strictly, so it produces an error such as: `method does not override or implement a method from a supertype`. As a result, the code fails to compile. D) Incorrect: It correctly identifies that the `Car` method is an overload rather than an override, but it incorrectly concludes that the code compiles. The `@Override` annotation prevents compilation because the annotated method is not actually overriding anything (see option C). If the `@Override` annotation were removed, then this option would describe the correct behavior. But with the annotation present, the code does not compile."
  - id: "ch02-011"
    title_fr: "Consider the following code snippet:\n```java\nclass Fruit {\npublic void flavor() {\nSystem.out.println(\"Fruit flavor\");\n}\n}\nclass Apple extends Fruit {\n@Override\npublic void flavor() {\nSystem.out.println(\"Apple flavor\");\n}\npublic void color() {\nSystem.out.println(\"Red\");\n}\n}\npublic class TestFruit {\npublic static void main(String[] args) {\nFruit myFruit = new Apple();\nmyFruit.flavor();\n// myFruit.color();\n}\n}\n```\nIf the commented line `// myFruit.color();` is uncommented, what will be the result of compiling and executing the above code?"
    title_en: "Consider the following code snippet:\n```java\nclass Fruit {\npublic void flavor() {\nSystem.out.println(\"Fruit flavor\");\n}\n}\nclass Apple extends Fruit {\n@Override\npublic void flavor() {\nSystem.out.println(\"Apple flavor\");\n}\npublic void color() {\nSystem.out.println(\"Red\");\n}\n}\npublic class TestFruit {\npublic static void main(String[] args) {\nFruit myFruit = new Apple();\nmyFruit.flavor();\n// myFruit.color();\n}\n}\n```\nIf the commented line `// myFruit.color();` is uncommented, what will be the result of compiling and executing the above code?"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "It compiles and prints `\"Apple flavor\"` followed by `\"Red\"`."
        text_en: "It compiles and prints `\"Apple flavor\"` followed by `\"Red\"`."
      - label: "B"
        text_fr: "It compiles and prints `\"Fruit flavor\"`."
        text_en: "It compiles and prints `\"Fruit flavor\"`."
      - label: "C"
        text_fr: "It compiles but throws a runtime exception when attempting to call `color()`."
        text_en: "It compiles but throws a runtime exception when attempting to call `color()`."
      - label: "D"
        text_fr: "It does not compile because `Apple` is not a valid type of `Fruit`."
        text_en: "It does not compile because `Apple` is not a valid type of `Fruit`."
      - label: "E"
        text_fr: "It does not compile because the `color` method is not defined in the `Fruit` class."
        text_en: "It does not compile because the `color` method is not defined in the `Fruit` class."
    correct_answers: ["E"]
    explanation_fr: "A) Incorrect: because, while the `flavor` method will indeed print `\"Apple flavor\"` due to polymorphism (the `Apple` class overrides the `flavor` method of `Fruit`), the code will not compile if the `color()` method is called on a `Fruit` reference. This is because the `color` method is not part of the `Fruit` class's interface. B) Incorrect: for a similar reason to A. The `flavor` method would print `\"Apple flavor\"` because of the overridden method in the `Apple` class, not `\"Fruit flavor\"`. However, the presence of the `color()` method call would still prevent compilation. C) Incorrect: because the issue occurs at compile time, not runtime. The Java compiler will not allow a method to be called on a reference type if that method is not defined in the reference type's class or its superclass hierarchy. D) Incorrect: `Apple` is a valid type of `Fruit` due to inheritance (`Apple extends Fruit`). This relationship allows an `Apple` object to be referenced by a `Fruit` variable. E) Correct: The `color` method is only defined in the `Apple` class and not in the `Fruit` class. Since the reference type of `myFruit` is `Fruit`, which does not have a `color` method, attempting to call `myFruit.color()` will result in a compilation error. This illustrates a key principle of polymorphism: the type of the reference (not the object) determines what methods can be called."
    explanation_en: "A) Incorrect: because, while the `flavor` method will indeed print `\"Apple flavor\"` due to polymorphism (the `Apple` class overrides the `flavor` method of `Fruit`), the code will not compile if the `color()` method is called on a `Fruit` reference. This is because the `color` method is not part of the `Fruit` class's interface. B) Incorrect: for a similar reason to A. The `flavor` method would print `\"Apple flavor\"` because of the overridden method in the `Apple` class, not `\"Fruit flavor\"`. However, the presence of the `color()` method call would still prevent compilation. C) Incorrect: because the issue occurs at compile time, not runtime. The Java compiler will not allow a method to be called on a reference type if that method is not defined in the reference type's class or its superclass hierarchy. D) Incorrect: `Apple` is a valid type of `Fruit` due to inheritance (`Apple extends Fruit`). This relationship allows an `Apple` object to be referenced by a `Fruit` variable. E) Correct: The `color` method is only defined in the `Apple` class and not in the `Fruit` class. Since the reference type of `myFruit` is `Fruit`, which does not have a `color` method, attempting to call `myFruit.color()` will result in a compilation error. This illustrates a key principle of polymorphism: the type of the reference (not the object) determines what methods can be called."
  - id: "ch02-012"
    title_fr: "Consider the following code snippet:\n```java\nclass Animal {}\nclass Dog extends Animal {\npublic void bark() {\nSystem.out.println(\"Woof\");\n}\n}\nclass Cat extends Animal {\npublic void meow() {\nSystem.out.println(\"Meow\");\n}\n}\npublic class TestCasting {\npublic static void main(String[] args) {\nAnimal animal = new Dog();\n((Dog)animal).bark();\nAnimal anotherAnimal = new Animal();\n// Line 1\n}\n}\n```\nWhich of the following lines of code, if inserted independently at Line 1, will compile without causing a runtime exception? (Choose all that apply.)"
    title_en: "Consider the following code snippet:\n```java\nclass Animal {}\nclass Dog extends Animal {\npublic void bark() {\nSystem.out.println(\"Woof\");\n}\n}\nclass Cat extends Animal {\npublic void meow() {\nSystem.out.println(\"Meow\");\n}\n}\npublic class TestCasting {\npublic static void main(String[] args) {\nAnimal animal = new Dog();\n((Dog)animal).bark();\nAnimal anotherAnimal = new Animal();\n// Line 1\n}\n}\n```\nWhich of the following lines of code, if inserted independently at Line 1, will compile without causing a runtime exception? (Choose all that apply.)"
    type: "multiple-choice"
    options:
      - label: "A"
        text_fr: "`((Dog)anotherAnimal).bark();`"
        text_en: "`((Dog)anotherAnimal).bark();`"
      - label: "B"
        text_fr: "`if (anotherAnimal instanceof Dog) ((Dog)anotherAnimal).bark();`"
        text_en: "`if (anotherAnimal instanceof Dog) ((Dog)anotherAnimal).bark();`"
      - label: "C"
        text_fr: "`((Cat)animal).meow();`"
        text_en: "`((Cat)animal).meow();`"
      - label: "D"
        text_fr: "`if (anotherAnimal instanceof Cat) ((Cat)anotherAnimal).meow();`"
        text_en: "`if (anotherAnimal instanceof Cat) ((Cat)anotherAnimal).meow();`"
    correct_answers: ["B", "D"]
    explanation_fr: "A) Incorrect: because it tries to cast `anotherAnimal` to `Dog` without checking its actual type first. Since `anotherAnimal` is an instance of `Animal` (not `Dog`), attempting this cast will compile, but it will cause a `ClassCastException` at runtime. B) Correct: It uses `instanceof` to check whether `anotherAnimal` is an instance of `Dog` before attempting the cast and calling `bark()`. In this case, since `anotherAnimal` is not an instance of `Dog`, the check prevents the cast and method call, avoiding a `ClassCastException`. C) Incorrect: because it casts `animal` to `Cat` and attempts to call `meow()`. Since `animal` is actually an instance of `Dog`, this cast will compile but will result in a `ClassCastException` at runtime. D) Correct: It checks if `anotherAnimal` is an instance of `Cat` before casting it to `Cat` and calling `meow()`."
    explanation_en: "A) Incorrect: because it tries to cast `anotherAnimal` to `Dog` without checking its actual type first. Since `anotherAnimal` is an instance of `Animal` (not `Dog`), attempting this cast will compile, but it will cause a `ClassCastException` at runtime. B) Correct: It uses `instanceof` to check whether `anotherAnimal` is an instance of `Dog` before attempting the cast and calling `bark()`. In this case, since `anotherAnimal` is not an instance of `Dog`, the check prevents the cast and method call, avoiding a `ClassCastException`. C) Incorrect: because it casts `animal` to `Cat` and attempts to call `meow()`. Since `animal` is actually an instance of `Dog`, this cast will compile but will result in a `ClassCastException` at runtime. D) Correct: It checks if `anotherAnimal` is an instance of `Cat` before casting it to `Cat` and calling `meow()`."
  - id: "ch02-013"
    title_fr: "Consider the following code snippet:\n```java\npublic class AdvancedPatternMatching {\npublic static void process(Object input) {\nif (input instanceof String s && s.contains(\"Java\")) {\nSystem.out.println(\"String with Java: \" + s);\n} else if (input instanceof Integer i && i > 10) {\nSystem.out.println(\"Integer greater than 10: \" + i);\n}\n}\npublic static void main(String[] args) {\nprocess(\"Hello Java!\");\nprocess(15);\nprocess(\"Just a string\");\nprocess(5);\n}\n}\n```\nGiven the above code, which statement accurately describes its execution result?"
    title_en: "Consider the following code snippet:\n```java\npublic class AdvancedPatternMatching {\npublic static void process(Object input) {\nif (input instanceof String s && s.contains(\"Java\")) {\nSystem.out.println(\"String with Java: \" + s);\n} else if (input instanceof Integer i && i > 10) {\nSystem.out.println(\"Integer greater than 10: \" + i);\n}\n}\npublic static void main(String[] args) {\nprocess(\"Hello Java!\");\nprocess(15);\nprocess(\"Just a string\");\nprocess(5);\n}\n}\n```\nGiven the above code, which statement accurately describes its execution result?"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "It compiles and prints `\"String with Java: Hello Java!\"` followed by `\"Integer greater than 10: 15\"`."
        text_en: "It compiles and prints `\"String with Java: Hello Java!\"` followed by `\"Integer greater than 10: 15\"`."
      - label: "B"
        text_fr: "It compiles but only prints `\"String with Java: Hello Java!\"` because integers are not supported with pattern matching."
        text_en: "It compiles but only prints `\"String with Java: Hello Java!\"` because integers are not supported with pattern matching."
      - label: "C"
        text_fr: "It does not compile because pattern matching in `instanceof` cannot be combined with logical operators like `&&`."
        text_en: "It does not compile because pattern matching in `instanceof` cannot be combined with logical operators like `&&`."
      - label: "D"
        text_fr: "It compiles but prints all four lines due to incorrect use of pattern matching that always evaluates to `true`."
        text_en: "It compiles but prints all four lines due to incorrect use of pattern matching that always evaluates to `true`."
    correct_answers: ["A"]
    explanation_fr: "A) Correct: The code snippet effectively demonstrates the use of pattern matching with the `instanceof` operator for both `String` and `Integer` types. The pattern matching feature checks if `input` is an instance of `String` or `Integer` and binds it to a variable (`s` for `String` and `i` for `Integer`) within the scope of the `if` and `else if` blocks. The logical operator `&&` is correctly used to further conditionally check properties of the variables (`s.contains(\"Java\")` and `i > 10`). Thus, the method `process` prints output for inputs that are a `String` containing `\"Java\"` and an `Integer` greater than `10`, respectively. B) Incorrect: because pattern matching works for any reference type, including `Integer`. The code does support integers and performs additional checks using pattern matching correctly. C) Incorrect: The code will compile and run as expected. Pattern matching in `instanceof` can indeed be combined with logical operators like `&&` for additional checks in the same conditional statement, as demonstrated in the code snippet. D) Incorrect: because the use of pattern matching in the provided code is correct and does not always evaluate to `true`. The code correctly prints specific messages only for the inputs that match the given conditions."
    explanation_en: "A) Correct: The code snippet effectively demonstrates the use of pattern matching with the `instanceof` operator for both `String` and `Integer` types. The pattern matching feature checks if `input` is an instance of `String` or `Integer` and binds it to a variable (`s` for `String` and `i` for `Integer`) within the scope of the `if` and `else if` blocks. The logical operator `&&` is correctly used to further conditionally check properties of the variables (`s.contains(\"Java\")` and `i > 10`). Thus, the method `process` prints output for inputs that are a `String` containing `\"Java\"` and an `Integer` greater than `10`, respectively. B) Incorrect: because pattern matching works for any reference type, including `Integer`. The code does support integers and performs additional checks using pattern matching correctly. C) Incorrect: The code will compile and run as expected. Pattern matching in `instanceof` can indeed be combined with logical operators like `&&` for additional checks in the same conditional statement, as demonstrated in the code snippet. D) Incorrect: because the use of pattern matching in the provided code is correct and does not always evaluate to `true`. The code correctly prints specific messages only for the inputs that match the given conditions."
  - id: "ch02-014"
    title_fr: "Consider the encapsulation practices in the following class structure:\n```java\npackage store;\npublic class Product {\nprivate String name;\nprivate double price;\nprivate int stock;\npublic Product(String name, double price, int stock) {\nsetName(name);\nsetPrice(price);\nsetStock(stock);\n}\npublic String getName() {\nreturn name;\n}\nprivate void setName(String name) {\nthis.name = name;\n}\npublic double getPrice() {\nreturn price;\n}\nprivate void setPrice(double price) {\nif (price >= 0) {\nthis.price = price;\n}\n}\npublic int getStock() {\nreturn stock;\n}\nprivate void setStock(int stock) {\nif (stock >= 0) {\nthis.stock = stock;\n}\n}\n}\n```\nWhich statement is true regarding the encapsulation of the `Product` class?"
    title_en: "Consider the encapsulation practices in the following class structure:\n```java\npackage store;\npublic class Product {\nprivate String name;\nprivate double price;\nprivate int stock;\npublic Product(String name, double price, int stock) {\nsetName(name);\nsetPrice(price);\nsetStock(stock);\n}\npublic String getName() {\nreturn name;\n}\nprivate void setName(String name) {\nthis.name = name;\n}\npublic double getPrice() {\nreturn price;\n}\nprivate void setPrice(double price) {\nif (price >= 0) {\nthis.price = price;\n}\n}\npublic int getStock() {\nreturn stock;\n}\nprivate void setStock(int stock) {\nif (stock >= 0) {\nthis.stock = stock;\n}\n}\n}\n```\nWhich statement is true regarding the encapsulation of the `Product` class?"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "Making the `setName`, `setPrice`, and `setStock` methods `public` would enhance the class's encapsulation."
        text_en: "Making the `setName`, `setPrice`, and `setStock` methods `public` would enhance the class's encapsulation."
      - label: "B"
        text_fr: "The class is not encapsulated because the `Product` class's fields are `private`."
        text_en: "The class is not encapsulated because the `Product` class's fields are `private`."
      - label: "C"
        text_fr: "Encapsulation is weakened because the constructor allows direct setting of fields without validation."
        text_en: "Encapsulation is weakened because the constructor allows direct setting of fields without validation."
      - label: "D"
        text_fr: "The `Product` class should have package-private getters to improve encapsulation."
        text_en: "The `Product` class should have package-private getters to improve encapsulation."
      - label: "E"
        text_fr: "The class is properly encapsulated by providing `public` getters for all fields and `private` setters with validation, ensuring control over the state of its objects."
        text_en: "The class is properly encapsulated by providing `public` getters for all fields and `private` setters with validation, ensuring control over the state of its objects."
    correct_answers: ["E"]
    explanation_fr: "A) Incorrect: Making the setters public would actually reduce the class's encapsulation by allowing external classes to modify the fields without restriction, potentially bypassing any validation logic contained within the setters. B) Incorrect: The use of `private` fields is a fundamental aspect of encapsulation. It prevents external classes from directly accessing and modifying the object's state, thus enforcing encapsulation. C) Incorrect: The constructor does not weaken encapsulation; instead, it uses `private` setters that contain validation logic. This ensures that the object's state is correctly managed and validated upon creation. D) Incorrect: Making getters package-private would limit the class's usability and does not inherently improve encapsulation. Public getters are necessary for external classes to view (but not modify) the object's state. E) Correct: The `Product` class demonstrates proper encapsulation practices by making its fields `private` and controlling access to them through `public` getters and `private` setters. The setters include validation logic, ensuring that only valid states are assigned to the fields. This design pattern ensures that the internal state of `Product` instances is both protected and correctly managed."
    explanation_en: "A) Incorrect: Making the setters public would actually reduce the class's encapsulation by allowing external classes to modify the fields without restriction, potentially bypassing any validation logic contained within the setters. B) Incorrect: The use of `private` fields is a fundamental aspect of encapsulation. It prevents external classes from directly accessing and modifying the object's state, thus enforcing encapsulation. C) Incorrect: The constructor does not weaken encapsulation; instead, it uses `private` setters that contain validation logic. This ensures that the object's state is correctly managed and validated upon creation. D) Incorrect: Making getters package-private would limit the class's usability and does not inherently improve encapsulation. Public getters are necessary for external classes to view (but not modify) the object's state. E) Correct: The `Product` class demonstrates proper encapsulation practices by making its fields `private` and controlling access to them through `public` getters and `private` setters. The setters include validation logic, ensuring that only valid states are assigned to the fields. This design pattern ensures that the internal state of `Product` instances is both protected and correctly managed."
  - id: "ch02-015"
    title_fr: "Consider the following classes defined in the same package:\n```java\nclass Account {\nprivate double balance;\nAccount(double initialBalance) {\nif (initialBalance > 0) {\nbalance = initialBalance;\n}\n}\nvoid deposit(double amount) {\nif (amount > 0) {\nbalance += amount;\n}\n}\nprotected double getBalance() {\nreturn balance;\n}\n}\npublic class SavingsAccount extends Account {\nprivate double interestRate;\npublic SavingsAccount(double initialBalance, double interestRate) {\nsuper(initialBalance);\nthis.interestRate = interestRate;\n}\npublic void applyInterest() {\ndouble interest = getBalance() * interestRate / 100;\ndeposit(interest);\n}\n}\n```\nWhich statement(s) about encapsulation principles and the use of access modifiers accurately describes the code above? Choose all tha apply."
    title_en: "Consider the following classes defined in the same package:\n```java\nclass Account {\nprivate double balance;\nAccount(double initialBalance) {\nif (initialBalance > 0) {\nbalance = initialBalance;\n}\n}\nvoid deposit(double amount) {\nif (amount > 0) {\nbalance += amount;\n}\n}\nprotected double getBalance() {\nreturn balance;\n}\n}\npublic class SavingsAccount extends Account {\nprivate double interestRate;\npublic SavingsAccount(double initialBalance, double interestRate) {\nsuper(initialBalance);\nthis.interestRate = interestRate;\n}\npublic void applyInterest() {\ndouble interest = getBalance() * interestRate / 100;\ndeposit(interest);\n}\n}\n```\nWhich statement(s) about encapsulation principles and the use of access modifiers accurately describes the code above? Choose all tha apply."
    type: "multiple-choice"
    options:
      - label: "A"
        text_fr: "The `SavingsAccount` class cannot access the `balance` field directly due to its `private` access modifier in the `Account` class."
        text_en: "The `SavingsAccount` class cannot access the `balance` field directly due to its `private` access modifier in the `Account` class."
      - label: "B"
        text_fr: "The `getBalance` method should be `public` to allow `SavingsAccount` to access the account balance."
        text_en: "The `getBalance` method should be `public` to allow `SavingsAccount` to access the account balance."
      - label: "C"
        text_fr: "The `deposit` method in the `Account` class should be marked as `final` to prevent overriding."
        text_en: "The `deposit` method in the `Account` class should be marked as `final` to prevent overriding."
      - label: "D"
        text_fr: "The `interestRate` field in the `SavingsAccount` class violates encapsulation principles by being `private`."
        text_en: "The `interestRate` field in the `SavingsAccount` class violates encapsulation principles by being `private`."
      - label: "E"
        text_fr: "The `Account` class correctly encapsulates the `balance` field, and `SavingsAccount` adheres to encapsulation by accessing `balance` through `getBalance` and `deposit`."
        text_en: "The `Account` class correctly encapsulates the `balance` field, and `SavingsAccount` adheres to encapsulation by accessing `balance` through `getBalance` and `deposit`."
    correct_answers: ["A", "E"]
    explanation_fr: "A) Correct: The design intentionally restricts direct access to the `balance` field to maintain encapsulation. B) Incorrect: Making `getBalance` `public` would increase its visibility unnecessarily. `protected` is sufficient for subclass access, and this change is not required for `SavingsAccount` to function correctly, making this statement incorrect. C) Incorrect: Marking `deposit` as `final` would prevent it from being overridden in subclasses, which is not a requirement or suggestion indicated by the given code. The decision to make a method `final` should be based on the specific design needs rather than a general principle of encapsulation. D) Incorrect: Using a `private` access modifier for `interestRate` in `SavingsAccount` is an example of proper encapsulation. It restricts access to the field from outside the class, which is aligned with encapsulation principles, making this option incorrect. E) Correct: The `Account` class uses `private` access for the `balance` field to encapsulate its state, providing `protected` and package-private methods (`getBalance` and `deposit`) for controlled access and modification. `SavingsAccount` respects this encapsulation by using these methods to interact with the `balance` field, demonstrating a proper understanding and application of encapsulation principles. This design allows `SavingsAccount` to leverage functionality provided by `Account` without breaking encapsulation, which is a key objective in object-oriented design."
    explanation_en: "A) Correct: The design intentionally restricts direct access to the `balance` field to maintain encapsulation. B) Incorrect: Making `getBalance` `public` would increase its visibility unnecessarily. `protected` is sufficient for subclass access, and this change is not required for `SavingsAccount` to function correctly, making this statement incorrect. C) Incorrect: Marking `deposit` as `final` would prevent it from being overridden in subclasses, which is not a requirement or suggestion indicated by the given code. The decision to make a method `final` should be based on the specific design needs rather than a general principle of encapsulation. D) Incorrect: Using a `private` access modifier for `interestRate` in `SavingsAccount` is an example of proper encapsulation. It restricts access to the field from outside the class, which is aligned with encapsulation principles, making this option incorrect. E) Correct: The `Account` class uses `private` access for the `balance` field to encapsulate its state, providing `protected` and package-private methods (`getBalance` and `deposit`) for controlled access and modification. `SavingsAccount` respects this encapsulation by using these methods to interact with the `balance` field, demonstrating a proper understanding and application of encapsulation principles. This design allows `SavingsAccount` to leverage functionality provided by `Account` without breaking encapsulation, which is a key objective in object-oriented design."
  - id: "ch02-016"
    title_fr: "Consider the following class:\n```java\npublic final class Contact {\nprivate final String name;\nprivate final String email;\nprivate final Address address;\npublic Contact(String name, String email, Address address) {\nthis.name = name;\nthis.email = email;\nthis.address = new Address(address.getStreet(), address.getCity());\n}\npublic String getName() {\nreturn name;\n}\npublic String getEmail() {\nreturn email;\n}\npublic Address getAddress() {\nreturn new Address(address.getStreet(), address.getCity());\n}\npublic static class Address {\nprivate final String street;\nprivate final String city;\npublic Address(String street, String city) {\nthis.street = street;\nthis.city = city;\n}\npublic String getStreet() {\nreturn street;\n}\npublic String getCity() {\nreturn city;\n}\n}\n}\n```\nGiven the above implementation, which statement accurately describes the `Contact` object?"
    title_en: "Consider the following class:\n```java\npublic final class Contact {\nprivate final String name;\nprivate final String email;\nprivate final Address address;\npublic Contact(String name, String email, Address address) {\nthis.name = name;\nthis.email = email;\nthis.address = new Address(address.getStreet(), address.getCity());\n}\npublic String getName() {\nreturn name;\n}\npublic String getEmail() {\nreturn email;\n}\npublic Address getAddress() {\nreturn new Address(address.getStreet(), address.getCity());\n}\npublic static class Address {\nprivate final String street;\nprivate final String city;\npublic Address(String street, String city) {\nthis.street = street;\nthis.city = city;\n}\npublic String getStreet() {\nreturn street;\n}\npublic String getCity() {\nreturn city;\n}\n}\n}\n```\nGiven the above implementation, which statement accurately describes the `Contact` object?"
    type: "single-choice"
    options:
      - label: "A"
        text_fr: "The `Contact` object is mutable because the `Address` class is not `final`."
        text_en: "The `Contact` object is mutable because the `Address` class is not `final`."
      - label: "B"
        text_fr: "The `Contact` object is immutable, but only because it does not provide setters."
        text_en: "The `Contact` object is immutable, but only because it does not provide setters."
      - label: "C"
        text_fr: "The `Contact` object is immutable, and it properly prevents leakage of mutable internal state through defensive copying."
        text_en: "The `Contact` object is immutable, and it properly prevents leakage of mutable internal state through defensive copying."
      - label: "D"
        text_fr: "The `Contact` object is mutable because the `Address` object can be changed via the `getAddress` method."
        text_en: "The `Contact` object is mutable because the `Address` object can be changed via the `getAddress` method."
      - label: "E"
        text_fr: "The `Contact` object is immutable but fails to prevent access to its mutable internal state."
        text_en: "The `Contact` object is immutable but fails to prevent access to its mutable internal state."
    correct_answers: ["C"]
    explanation_fr: "A) Incorrect: because the `Address` class does not directly impact the immutability of the `Contact` object. The `Contact` class ensures its immutability by not providing setters and by making deep copies of mutable objects, such as `Address`, both in the constructor and the getter. B) Incorrect: While it's true that it does not provide setters, this option does not fully capture the essence of immutability. Thus, it doesn't highlight the fact that all fields in `Contact` are `final` and the defensive copying strategy. C) Correct: option. The `Contact` class is immutable because it meets all criteria for immutability: the class is declared as `final` (preventing subclassing), all its fields are `private` and `final`, and it does not provide any setters. Also, it implements defensive copying for the mutable `Address` field to ensure that the internal state cannot be altered by external changes to `Address` objects passed in or returned. This prevents the leakage of its mutable internal state. D) Incorrect: because the `Contact` object's immutability is maintained through defensive copying. The `getAddress` method returns a new `Address` instance each time it is called, ensuring that the original `Address` object's state cannot be altered from outside the `Contact` object. E) Incorrect: because the `Contact` object does implement a strategy to prevent access to its mutable internal state: it uses defensive copying for the `Address` object in both the constructor and the getter method, which ensures that the internal state remains unchanged from outside modifications."
    explanation_en: "A) Incorrect: because the `Address` class does not directly impact the immutability of the `Contact` object. The `Contact` class ensures its immutability by not providing setters and by making deep copies of mutable objects, such as `Address`, both in the constructor and the getter. B) Incorrect: While it's true that it does not provide setters, this option does not fully capture the essence of immutability. Thus, it doesn't highlight the fact that all fields in `Contact` are `final` and the defensive copying strategy. C) Correct: option. The `Contact` class is immutable because it meets all criteria for immutability: the class is declared as `final` (preventing subclassing), all its fields are `private` and `final`, and it does not provide any setters. Also, it implements defensive copying for the mutable `Address` field to ensure that the internal state cannot be altered by external changes to `Address` objects passed in or returned. This prevents the leakage of its mutable internal state. D) Incorrect: because the `Contact` object's immutability is maintained through defensive copying. The `getAddress` method returns a new `Address` instance each time it is called, ensuring that the original `Address` object's state cannot be altered from outside the `Contact` object. E) Incorrect: because the `Contact` object does implement a strategy to prevent access to its mutable internal state: it uses defensive copying for the `Address` object in both the constructor and the getter method, which ensures that the internal state remains unchanged from outside modifications."
---

Chapter 2 - Utilizing Java Object-Oriented Approach - Part 2 exam.